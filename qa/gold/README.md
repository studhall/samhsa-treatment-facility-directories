# Gold review results

`gold_sample.csv` is the tracked authoritative export from the completed Excel
review. `gold_evaluation_by_year.csv` and `gold_evaluation.json` are regenerated
against a selected full parsing run.
